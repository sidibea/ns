{% if locales|length > 1 %}
    <li class="dropdown dropdown-small"> <a href="#" class="dropdown-toggle" data-hover="dropdown" data-toggle="dropdown"><span class="value">{{ active|sylius_locale_name }} </span><b class="caret"></b></a>
        <ul class="dropdown-menu">
            {% for code in locales %}
                <li><a href="{{ path('sylius_shop_switch_locale', {'code': code}) }}">{{ code|sylius_locale_name }}</a></li>
            {% endfor %}
        </ul>
    </li>
{% endif %}